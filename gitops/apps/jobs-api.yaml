apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jobs-api
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Acoolwolf96/azure-platform-sre.git
    targetRevision: main
    path: gitops/jobs-api

  destination:
    server: https://kubernetes.default.svc
    namespace: jobs

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
